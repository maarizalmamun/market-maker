/// <reference types="node" />
import { DataAndSlot, UserAccountEvents, UserAccountSubscriber } from '@drift-labs/sdk/lib/accounts/types';
import { Program } from '@coral-xyz/anchor';
import StrictEventEmitter from 'strict-event-emitter-types';
import { EventEmitter } from 'events';
import { PublicKey } from '@solana/web3.js';
import { UserAccount } from '@drift-labs/sdk/lib/types';
import { BulkAccountLoader } from '@drift-labs/sdk/lib/accounts/bulkAccountLoader';
export declare class PollingUserAccountSubscriber implements UserAccountSubscriber {
    isSubscribed: boolean;
    program: Program;
    eventEmitter: StrictEventEmitter<EventEmitter, UserAccountEvents>;
    userAccountPublicKey: PublicKey;
    accountLoader: BulkAccountLoader;
    callbackId?: string;
    errorCallbackId?: string;
    user?: DataAndSlot<UserAccount>;
    constructor(program: Program, userAccountPublicKey: PublicKey, accountLoader: BulkAccountLoader);
    subscribe(userAccount?: UserAccount): Promise<boolean>;
    addToAccountLoader(): Promise<void>;
    fetchIfUnloaded(): Promise<void>;
    fetch(): Promise<void>;
    doesAccountExist(): boolean;
    unsubscribe(): Promise<void>;
    assertIsSubscribed(): void;
    getUserAccountAndSlot(): DataAndSlot<UserAccount>;
    updateData(userAccount: UserAccount, slot: number): void;
}
