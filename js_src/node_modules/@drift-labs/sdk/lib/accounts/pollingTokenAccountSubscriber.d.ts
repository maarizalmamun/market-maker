/// <reference types="node" />
import { DataAndSlot, TokenAccountEvents, TokenAccountSubscriber } from '@drift-labs/sdk/lib/accounts/types';
import { Program } from '@coral-xyz/anchor';
import StrictEventEmitter from 'strict-event-emitter-types';
import { EventEmitter } from 'events';
import { PublicKey } from '@solana/web3.js';
import { BulkAccountLoader } from '@drift-labs/sdk/lib/accounts/bulkAccountLoader';
import { AccountInfo } from '@solana/spl-token';
export declare class PollingTokenAccountSubscriber implements TokenAccountSubscriber {
    isSubscribed: boolean;
    program: Program;
    eventEmitter: StrictEventEmitter<EventEmitter, TokenAccountEvents>;
    publicKey: PublicKey;
    accountLoader: BulkAccountLoader;
    callbackId?: string;
    errorCallbackId?: string;
    tokenAccountAndSlot?: DataAndSlot<AccountInfo>;
    constructor(publicKey: PublicKey, accountLoader: BulkAccountLoader);
    subscribe(): Promise<boolean>;
    addToAccountLoader(): Promise<void>;
    fetch(): Promise<void>;
    unsubscribe(): Promise<void>;
    assertIsSubscribed(): void;
    getTokenAccountAndSlot(): DataAndSlot<AccountInfo>;
    didSubscriptionSucceed(): boolean;
}
