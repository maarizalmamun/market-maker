import { ConfirmOptions, Connection, PublicKey } from '@solana/web3.js';
import { IWallet } from '@drift-labs/sdk/lib/types';
import { OracleInfo } from '@drift-labs/sdk/lib/oracles/types';
import { BulkAccountLoader } from '@drift-labs/sdk/lib/accounts/bulkAccountLoader';
import { DriftEnv } from '@drift-labs/sdk/lib/config';
export type DriftClientConfig = {
    connection: Connection;
    wallet: IWallet;
    programID: PublicKey;
    accountSubscription?: DriftClientSubscriptionConfig;
    opts?: ConfirmOptions;
    txSenderConfig?: TxSenderConfig;
    subAccountIds?: number[];
    activeSubAccountId?: number;
    perpMarketIndexes?: number[];
    spotMarketIndexes?: number[];
    marketLookupTable?: PublicKey;
    oracleInfos?: OracleInfo[];
    env?: DriftEnv;
    userStats?: boolean;
    authority?: PublicKey;
    includeDelegates?: boolean;
    authoritySubAccountMap?: Map<string, number[]>;
    skipLoadUsers?: boolean;
};
export type DriftClientSubscriptionConfig = {
    type: 'websocket';
} | {
    type: 'polling';
    accountLoader: BulkAccountLoader;
};
type TxSenderConfig = {
    type: 'retry';
    timeout?: number;
    retrySleep?: number;
    additionalConnections?: Connection[];
};
export {};
